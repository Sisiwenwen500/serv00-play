name: Sync Fork with Upstream

on:
  # ⚠️ 强烈建议注释掉定时同步！
  # 因为你修改了核心逻辑，自动同步极有可能覆盖你的修改，导致 small.pl 无法使用。
  # schedule:
  #   - cron: '0 19 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync_latest_from_upstream:
    name: Sync latest commits from upstream repo
    runs-on: ubuntu-latest
    if: ${{ github.event.repository.fork }}

    steps:
      # Step 1: run a standard checkout action
      - name: Checkout target repo
        uses: actions/checkout@v3

      # Step 2: run the sync action
      - name: Sync upstream changes
        id: sync
        uses: aormsby/Fork-Sync-With-Upstream-action@v3.4
        with:
          upstream_sync_repo: frankiejun/serv00-play
          upstream_sync_branch: main
          target_sync_branch: main
          target_repo_token: ${{ secrets.GITHUB_TOKEN }} # automatically generated, no need to set
          
          # 🔥 关键修改：排除所有你修改过的文件 🔥
          # 这样同步时会忽略原作者对这些文件的更改，保护你的 small.pl 适配代码
          exclude: |
            .github/workflows/sync.yml
            .github/workflows/deploy.yml
            .github/workflows/keep.yml
            start.sh
            utils.sh
            keepalive.sh
            revive.sh
            revive_node.sh
            singbox/start.sh

      - name: Check for errors
        if: failure()
        run: echo "Sync failed! Please check the logs for more details."

      - name: Sync success message
        if: success()
        run: echo "Sync completed successfully!"
